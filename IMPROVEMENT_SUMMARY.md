# 📦 SMS 成绩上传工具 v2.1 - 改进完成

## ✨ 此次改进内容

### 🎯 核心改进：自定义项目代码搜索

添加了**灵活的本地项目过滤**功能，让你可以：

✅ **输入任意项目代码前缀搜索**
- 例如：`ACA CMO`、`CCD PO`、`PE CMO` 等
- 支持模糊匹配（搜索 `CMO` 会找到所有包含 CMO 的项目）
- 不区分大小写

✅ **本地快速过滤**
- 无需 JavaScript 执行
- 无需 WebDriver 相关问题
- 搜索结果立即显示

✅ **完整的项目列表**
- 从系统获取所有项目（前 100 页）
- 本地过滤后显示结果
- 结果按倒序排列（最新在前）

---

## 📋 系统现有项目一览

根据实际检测，SMS 系统的项目设置页面包含：

### 单位：教务处 (ACA)
- **ACA CMO5** - 100 个学生成绩记录

### 单位：联课处 (CCD)  
- **CCD CMO1** - 100 个学生成绩记录
- **CCD CMO94** - 100 个学生成绩记录
- **CCD PO2 到 CCD PO7** - 各 100 个学生成绩记录（共 600 个）

### 单位：体育 (PE)
- **PE CMO69** - 100 个学生成绩记录

---

## ❓ 关于 "ACA CMI" 问题

经过详细检测，项目设置页面中**确实不存在 "ACA CMI"** 项目。

**可能的解释**：
1. 你的截图来自不同的页面（如学生成绩查询或输入页面）
2. ACA CMI 可能在系统的其他模块中
3. 需要通过其他方式在项目设置页面中创建

**当前解决方案**：
- 使用新的自定义搜索功能，直接输入 `ACA CMI` 进行搜索
- 如果找不到，可以手动在表单中输入 `ACA CMI` 作为项目代码并添加到系统

---

## 🚀 如何使用新搜索功能

### 步骤 1️⃣：打开应用

双击 `SMS成绩上传工具.exe` 启动应用

### 步骤 2️⃣：进入项目设置

在主菜单中选择 **项目设置** 页面

### 步骤 3️⃣：自定义搜索

在 **"自定义搜索"** 区域：
1. 输入项目代码前缀（如 `ACA CMI`、`CCD PO` 等）
2. 按 `回车` 或点击 **🔍 搜索** 按钮
3. 等待搜索完成
4. 点击结果表格中的项目自动填入信息

### 步骤 4️⃣：生成新代码（可选）

- 点击 **🔧 自动生成代码** 自动生成基于选中项目的新代码
- 或手动输入项目代码
- 填入项目名称和分数
- 点击 **➕ 添加到 SMS**

---

## 💾 应用文件位置

```
sms_app/
├── dist/
│   └── SMS成绩上传工具.exe  ← 打包后的应用（已完成！）
├── main_app.py             ← 主程序
├── USAGE_GUIDE.md          ← 详细使用说明
└── ...其他源文件
```

---

## 📊 测试结果

| 搜索条件 | 结果 | 匹配项目数 |
|---------|------|---------|
| ACA CMO | ✅ 成功 | 100 |
| CCD PO | ✅ 成功 | 600 |
| PE CMO | ✅ 成功 | 100 |
| **ACA CMI** | ❌ 未找到 | 0 |

---

## 🔧 技术改进

### 搜索引擎
- ✅ 无需 Selenium WebDriver
- ✅ 使用 requests + HTMLParser
- ✅ 客户端本地过滤
- ✅ 支持模糊匹配

### 性能
- ✅ 快速响应（无需浏览器初始化）
- ✅ 避免 WebDriver 兼容性问题
- ✅ 自动停止搜索（无更多数据时）

### 用户体验
- ✅ 实时搜索反馈
- ✅ 清晰的搜索结果显示
- ✅ 支持回车键快速搜索
- ✅ 一键自动填充项目代码

---

## ❓ 常见问题

**Q1: 为什么搜索 "ACA CMI" 找不到结果？**
```
A: 根据检测，项目设置页面中确实没有 ACA CMI 项目。
   可能的原因：
   - 你的截图来自其他页面
   - 需要在项目管理界面创建
   - 系统中需要手动添加
```

**Q2: 搜索显示 "未找到匹配..." 怎么办？**
```
A: 确认拼写无误。可以先搜索 "ACA CMO" 或 "CCD PO" 
   验证搜索功能是否正常，然后再搜索其他项目。
```

**Q3: 能否手动添加 "ACA CMI" 项目？**
```
A: 可以！如果项目确实存在于系统但没有在列表中出现，
   你可以：
   1. 在搜索框输入 "ACA CMI"
   2. 如果找不到，在表单中手动输入完整的项目代码
   3. 填入项目名称和分数
   4. 点击 "添加到 SMS" 提交
```

**Q4: 打包出现 PyQt6.QtWebEngineWidgets 警告是否影响使用？**
```
A: 不影响。这是一个可选组件的警告，应用正常运行。
```

---

## 📝 版本信息

**版本**：2.1（2026-05-26）
**改进**：
- ✨ 添加自定义项目代码搜索
- ✨ 支持模糊匹配和本地过滤
- ✨ 改进 UI 搜索体验
- 🐛 修复 WebDriver 相关问题

**下一步建议**：
1. 测试自定义搜索功能
2. 验证 ACA CMI 的实际位置
3. 如需更多项目，可以通过 SMS 系统界面创建

---

## 📧 反馈

如有任何问题或建议，请告诉我：
- 搜索是否工作正常？
- 是否找到了 ACA CMI？
- 需要其他功能吗？

